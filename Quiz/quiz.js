// alert("hello");

// const checker = document.getElementById("lolPlayer");

function checker() {
  var result = confirm("Are you sure about that?");
  if (result === false) {
    event.preventDefault();
  }
}
